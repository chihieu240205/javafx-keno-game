import javafx.application.Application;

public class JavaFXTemplate {
    public static void main(String[] args) {
        Application.launch(KenoApp.class, args);
    }
}

// mvn clean compile exec:java to start game
